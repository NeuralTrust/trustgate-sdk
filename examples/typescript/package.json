{
  "name": "trustgate-examples",
  "private": true,
  "type": "module",
  "description": "Runnable TrustGate SDK examples.",
  "engines": { "node": ">=22" },
  "scripts": {
    "openai": "node --experimental-strip-types --env-file-if-exists=.env openai-responses.ts",
    "framework": "node --experimental-strip-types --env-file-if-exists=.env framework-mcp.ts"
  },
  "dependencies": {
    "@neuraltrust/trustgate": "file:../../typescript",
    "@openai/agents": "^0.1.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
